# Must be the real absolute URL, not "/": og:url and og:image are built with
# absURL, and a relative value there means crawlers (LinkedIn, Slack, Twitter)
# cannot fetch the share card. www.maxspohn.com 301s to the apex, so the apex is
# canonical. netlify.toml overrides this with -b $DEPLOY_PRIME_URL for branch
# and preview builds, so those stay correct.
baseURL = "https://maxspohn.com/"
theme = "hugo-apero"
languageCode = "en"
title = "Max Spohn"
author = "Max Spohn"
copyright = "" # set to override the auto generated copyright using org info and now year
googleAnalytics = ""
disqusShortname = "" # see params.utterances to use utterances instead of Disqus
metaDataFormat = "yaml"
footnoteReturnLinkContents = "&#8617;" # ↩
# set deliberately low for testing choose your preffered number based on the blog layout you've chosen
paginate = 5
preserveTaxonomyNames = true
ignoreFiles = ["\\.Rmd$", "\\.Rmarkdown$", "_cache$", "\\.knit\\.md$", "\\.utf8\\.md$"]
enableEmoji = true
# Hugo lowercases menu URLs by default, which turned the CV link into
# /cv_spohn.pdf. That resolves on Windows but 404s on Netlify's case-sensitive
# filesystem. Verified: enabling this changes nothing else in the built output.
disablePathToLower = true

[markup]
  defaultMarkdownHandler = "goldmark"
  [markup.goldmark.renderer]
    unsafe = true  # Enable user to embed HTML snippets in Markdown content.
    hardwraps = false 
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 2
    ordered = false
  [markup.highlight]
    # one of: https://xyproto.github.io/splash/docs/ 
    style = "github"
    
[params]
  orgName = "Max Spohn"
  orgLocal = ""
  description = "PhD candidate in Public Policy at Harvard Kennedy School. I study evidence adoption in government and organizations, and interventions to reduce polarization."
  favicon = "/img/algorithm.png"
  # logo = "/img/blogophonic-mark-dark.png"
  mainSections = ["research"]
  navallcaps = true
  # show/hide social icons in site header & footer
  # configure social icons and links below in [[params.social]]
  socialInHeader = false
  socialInFooter = true
  # Default image for social sharing and search engines. 
  # Place image file in `static` folder and specify image name here.
  sharing_image = "/img/_MG_9143.jpg"
  # Twitter username (without @). Used when a visitor shares your site on Twitter.
  twitter = "MaxSpohn"
  
  # use a built-in color theme
  # one of: earth / forest / grayscale / magma / paper / peach / plum /
  #         poppy / primer / sky / violet / water 
  theme = "sky"
  
  # or, leave theme empty & make your own palette
  # see docs at https://hugo-apero.netlify.app/blog/color-themes/
  # the custom scss file must be in the assets/ folder
  # add the filename name here, without extension
  # to use hex colors instead of named tachyons colors, include "hex" in filename
  custom_theme = "" 
  
  # use an embedded font-family
  # serif options: Fraunces / EB Garamond / Bitter
  # sans-serif options: Commissioner / Atkinson Hyperlegible / Metropolis
  # if you import your own fonts, name them here too
  customtextFontFamily = "Commissioner"
  customheadingFontFamily = "Fraunces"
  # alternatively, leave empty and select basic font options: sans-serif or serif
  # for more http://tachyons.io/docs/typography/font-family/
  textFontFamily = "sans-serif"
  headingFontFamily = "sans-serif"

  # Configuration of https://utteranc.es/ for comments
  [params.utterances]
    use_utterances = true
    repo_name = "apreshill/apero"
    issue_term = "pathname" # url, pathname, or title
    theme = "boxy-light"
    label = "comments :crystal_ball:"

  # Configuration of math rendering
  [params.math]
    renderer = "katex" # one of "mathjax" / "katex" 

  # Social icons may appear on homepage and in site header or footer
  # Configure show/hide above - add as many as you like below
  # Icon pack "fab" includes brand icons, see: https://fontawesome.com/icons?d=gallery&s=brands&m=free
  # Icon pack "fas" includes solid icons, see: https://fontawesome.com/icons?d=gallery&s=solid&m=free
  # Icon pack "far" includes regular icons, see: https://fontawesome.com/icons?d=gallery&s=regular&m=free
  # `icon` + `icon_pack` uses Font Awesome 5.15.1 ("fas"/"fab") or academicons
  # ("ai"). Neither ships an X or Bluesky mark -- both postdate FA 5.15.1 -- so
  # those two use `svg` instead, rendered inline by
  # layouts/partials/shared/social-links.html.
  [[params.social]]
      icon      = "envelope"
      icon_pack = "fas"
      name      = "Email"
      url       = "mailto:hspohn@g.harvard.edu"
  [[params.social]]
      icon      = "google-scholar"
      icon_pack = "ai"
      name      = "Google Scholar"
      url       = "https://scholar.google.com/citations?user=6bedqfAAAAAJ&hl=en"
  [[params.social]]
      icon      = "linkedin"
      icon_pack = "fab"
      name      = "LinkedIn"
      url       = "https://www.linkedin.com/in/max-spohn/"
  [[params.social]]
      svg       = "x"
      name      = "X"
      url       = "https://x.com/MaxSpohn"
  [[params.social]]
      svg       = "bluesky"
      name      = "Bluesky"
      url       = "https://bsky.app/profile/maxspohn.bsky.social"

[taxonomies]
  category = "categories"
  series = "series"
  tag = "tags"

[menu]
  # Configure header
  [[menu.header]]
    name = "Home"
    title = "Home"
    url = "/"
    weight = 1
  [[menu.header]]
    name = "Research"
    title = "Research"
    url = "/research/"
    weight = 2
  [[menu.header]]
    name = "CV"
    title = "CV"
    url = "/CV_Spohn.pdf"
    weight = 3
    
  # Configure footer  
#  [[menu.footer]]
#    name = "License"
#    title = "License"
#    url = "/license/"
#    weight = 1
#  [[menu.footer]]
#    name = "Contact"
#    title = "Contact form"
#    url = "/contact/"
#    weight = 1
#  [[menu.footer]]
#    name = "Contributors"
#    title = "Contributors"
#    url = "/contributors/"
#    weight = 3

# see https://gohugo.io/about/hugo-and-gdpr/    
[privacy]
  [privacy.disqus]
    disable = false
  [privacy.googleAnalytics]
    disable = false
    anonymizeIP = true
    respectDoNotTrack = true
  [privacy.instagram]
    disable = false
    simple = true
  [privacy.twitter]
    disable = false
    enableDNT = true
    simple = false
  [privacy.vimeo]
    disable = false
    enableDNT = true
    simple = true
  [privacy.youtube]
    disable = false
    privacyEnhanced = true
